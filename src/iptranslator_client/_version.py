# Bump together with the release tag (release.yml refuses a tag that does not
# match). Versions track the .NET client (ipt-client-dotnet) — both speak the
# same protocol version.
__version__ = "2.1.0"
